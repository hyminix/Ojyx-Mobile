# Production build configuration for Ojyx
# Format: MODE|DESCRIPTION|EXTRA_FLAGS

# Standard builds
debug|Development build with debugging enabled|--verbose
profile|Performance profiling build|--low-memory
release|Production release build|--clean-gradle

# Special builds
release|Play Store release build|--clean-gradle --target-platform android-arm64
release|APK for testing|--split-per-abi